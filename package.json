{
  "name": "facebook-google-calendar-event-exporter",
  "version": "1.0.0",
  "description": "Browser extension that adds a 'Export to Google Calendar' option to Facebook's event export modal",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "webpack --mode production",
    "dev": "webpack --mode development --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src/",
    "clean": "rimraf dist"
  },
  "keywords": [
    "facebook",
    "google-calendar",
    "browser-extension",
    "ics",
    "calendar"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}
